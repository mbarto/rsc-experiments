# RSC Experiment: SSR example with optimized HTML + RSC using distinct backends

This is the pages example implementing an SSR solution with both HTML and RSC generation using two dedicated backends.
